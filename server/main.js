import { Meteor } from 'meteor/meteor';
import '/imports/api/sounds.js';
import '/imports/api/songs.js';
import '/imports/api/tones.js';

Meteor.startup(() => {
  // code to run on server at startup
});
